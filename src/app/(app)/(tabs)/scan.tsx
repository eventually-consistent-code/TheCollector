/**
 * Purpose: Dummy route behind the raised Scan tab. Never rendered — the
 * tab's press is intercepted in the layout and opens the collection
 * picker instead of navigating here.
 * Author(s): John Reed
 */

export default function ScanTabPlaceholder() {
  return null;
}
