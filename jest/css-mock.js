/**
 * Purpose: Empty stand-in for CSS imports (theme.ts pulls in global.css for
 * web font vars) — Jest can't parse stylesheets and never needs to.
 * Author(s): John Reed
 */

module.exports = {};
